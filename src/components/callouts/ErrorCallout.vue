<template>
    <div class="callout-container accessibility-pattern-diagonal">
        <p>Error Callout</p>
    </div>
</template>

<style scoped>
.callout-container {
    background-color: var(--colour-red-faded);
    color: var(--colour-red);
    border-radius: var(--border-radius);
    border-width: var(--border-width);
    border-color: var(--colour-red);
    border-style: solid;
    transition: all 0.3s ease;
    padding: 8px 24px;
    user-select: none;
}
</style>